django>=5.1,<6.0
djangorestframework>=3.15
celery>=5.4
redis>=5.0
playwright==1.49.0
aiohttp>=3.11
jinja2>=3.1
python-dotenv>=1.0
django-cors-headers>=4.4
whitenoise>=6.8
gunicorn>=23.0

# --- Sentinel security / agent additions ---
argon2-cffi>=23.1          # Argon2id password hashing
cryptography>=43.0         # Fernet field encryption + blind-index primitives
requests>=2.32             # outbound HTTP for exposure sources (HIBP, DeHashed, search)
pyotp>=2.9                 # TOTP multi-factor authentication
pywebpush>=2.0             # Web Push (VAPID) browser notifications
