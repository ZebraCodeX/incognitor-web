target-version = "py312"
line-length = 100
extend-exclude = [".venv", "migrations", "staticfiles"]

[lint]
select = ["E", "F", "I", "W"]
ignore = [
  "E501",  # line length handled by formatter/limits
  "E402",  # module-level imports not at top (env/dotenv ordering)
]
